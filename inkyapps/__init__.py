"""Mini-apps for a Pimoroni Inky Impression driven by a Raspberry Pi.

This file just marks the directory as a package - it deliberately contains no
imports, so that importing `inkyapps.layout` on a laptop doesn't drag in
hardware-only modules like `inkyapps.display`.
"""
